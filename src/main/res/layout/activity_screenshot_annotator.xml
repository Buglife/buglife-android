<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:animateLayoutChanges="true"
    android:background="@android:color/black"
    tools:ignore="Overdraw">

    <com.buglife.sdk.AnnotationView2
        android:id="@+id/annotation_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        />

    <LinearLayout
        android:id="@+id/annotation_toolbar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_gravity="bottom">

        <ImageButton
            android:id="@+id/arrow_tool"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_margin="6dp"
            android:layout_weight="1"
            android:scaleType="fitCenter"
            android:padding="2dp"
            android:background="@drawable/annotation_tool"
            android:src="@drawable/arrow_icon"
            />

        <ImageButton
            android:id="@+id/loupe_tool"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_margin="6dp"
            android:layout_weight="1"
            android:scaleType="fitCenter"
            android:padding="6dp"
            android:background="@drawable/annotation_tool"
            android:src="@drawable/loupe_icon"
            />

        <ImageButton
            android:id="@+id/blur_tool"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_margin="6dp"
            android:layout_weight="1"
            android:scaleType="fitCenter"
            android:padding="6dp"
            android:background="@drawable/annotation_tool"
            android:src="@drawable/pixelate_icon"
            />

    </LinearLayout>

</FrameLayout>